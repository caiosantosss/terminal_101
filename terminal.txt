pwd
=> predent working directory

ls
=> lists content of a folder

ll
=> long list

cd move-name
=> move into a folder

cd ..
=> moves back to parent folder

mkdir folder_name
=> creates new folder

rm filname
=> removes a file

rm -f
=> force removes a file

rm -rf folder_name
=> removes a folder

mv filename folder_name
=> move a file into a folder

mv index.html home.html
=> rename de files

code .
=> open the folder in vs code
